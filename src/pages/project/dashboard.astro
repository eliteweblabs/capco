---
const requireAuthRedirect = "/auth/login";
import { checkAuth } from "../../lib/auth";
const { currentUser, session, supabase, refreshToken, accessToken } = await checkAuth(
  Astro.cookies
);
const currentRole = currentUser?.profile?.role || "Client";

// Handle auth redirect first
if (requireAuthRedirect && (!session || !currentUser)) {
  return Astro.redirect(requireAuthRedirect);
}

import { globalCompanyData } from "../api/global/global-company-data.ts";
const {
  globalCompanyName,
  globalCompanySlogan,
  globalCompanyAddress,
  globalCompanyPhone,
  globalCompanyEmail,
  globalCompanyWebsite,
  globalCompanyLogo,
} = await globalCompanyData();

import { globalClasses } from "../api/global/global-classes.ts";
const { globalInputClasses, primaryTextClasses, secondaryTextClasses } = globalClasses();

import App from "../../components/ui/App.astro";
import DashboardContent from "../../components/project/Dashboard.astro";
import "../../lib/refresh-manager.ts";
import type { Project } from "../../lib/api/.ts";
// Get auth data directly since App component handles auth requirements

// console.log("🚀 [DASHBOARD] Current user:", currentUser);
// Initialize data arrays
let projects: Project[] = [];

// Fetch projects from the API endpoint
if (currentUser?.id) {
  try {
    const apiUrl = new URL("/api/projects/get", Astro.url.origin);
    
    // For clients, filter by authorId
    if (currentRole === "Client") {
      apiUrl.searchParams.set("authorId", currentUser.id);
    }
    
    // Only load first 50 projects for faster initial render
    apiUrl.searchParams.set("limit", "50");
    
    // Add a timeout to prevent hanging
    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), 8000); // 8 second timeout
    
    try {
      const startTime = Date.now();
      const response = await fetch(apiUrl.toString(), {
        method: "GET",
        headers: {
          "Content-Type": "application/json",
          Cookie: Astro.request.headers.get("Cookie") || "",
        },
        signal: controller.signal,
      });

      clearTimeout(timeoutId);
      const elapsed = Date.now() - startTime;

      if (response.ok) {
        const data = await response.json();
        projects = data.projects || [];
        console.log(`🏗️ [PROJECT/DASHBOARD] Loaded ${projects.length} projects in ${elapsed}ms`);
      } else {
        console.error("🏗️ [PROJECT/DASHBOARD] API error:", response.status);
      }
    } catch (fetchError) {
      clearTimeout(timeoutId);
      if (fetchError.name === 'AbortError') {
        console.error("🏗️ [PROJECT/DASHBOARD] Request timeout after 8s - loading with empty projects");
      } else {
        throw fetchError;
      }
    }
  } catch (error) {
    console.error("🏗️ [PROJECT/DASHBOARD] Error fetching projects:", error);
  }
}

// Fetch project statuses using the unified API
const statusApiUrl = new URL("/api/status/get", Astro.url.origin);
const statusResponse = await fetch(statusApiUrl.toString(), {
  method: "GET",
  headers: {
    Cookie: Astro.request.headers.get("Cookie") || "",
  },
});

const statusResponseData = await statusResponse.json();
const statusData = statusResponseData.statuses || [];
---

<App
  title="Global Dashboard"
  description="View all projects."
  {currentUser}
  {session}
  {supabase}
  {projects}
  {globalCompanyName}
  {globalCompanySlogan}
  {primaryTextClasses}
  {secondaryTextClasses}
  {globalInputClasses}
  {globalCompanyAddress}
  {globalCompanyPhone}
  {globalCompanyEmail}
  {globalCompanyWebsite}
  {globalCompanyLogo}
  {statusData}
  mainClasses="overflow-x-scroll"
>
  <DashboardContent
    projects={projects}
    statusData={statusData}
    currentUser={currentUser}
    globalInputClasses={globalInputClasses}
    secondaryTextClasses={secondaryTextClasses}
    primaryTextClasses={primaryTextClasses}
  />
</App>

<script>
  // Start the refresh manager to poll for updates
  import { refreshManager } from "../../lib/refresh-manager";
  
  console.log("🔄 [DASHBOARD] Starting refresh manager...");
  
  // Wait for DOM to be fully loaded
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", () => {
      console.log("🔄 [DASHBOARD] DOM loaded, initializing refresh manager");
      refreshManager.startAutoRefreshWithInterval(10); // Poll every 10 seconds (safer than 5)
      console.log("🔄 [DASHBOARD] Refresh manager started:", refreshManager.getRefreshStats());
    });
  } else {
    console.log("🔄 [DASHBOARD] DOM already loaded, initializing refresh manager");
    refreshManager.startAutoRefreshWithInterval(10); // Poll every 10 seconds (safer than 5)
    console.log("🔄 [DASHBOARD] Refresh manager started:", refreshManager.getRefreshStats());
  }
</script>
