---
/**
 * Hybrid Contact Page
 * Offers users a choice between conversational chat interface or traditional form
 */

import App from "../components/ui/App.astro";
import VapiChatInterface from "../components/chat/VapiChatInterface.astro";
import MultiStepForm from "../components/form/MultiStepForm.astro";
import { getContactFormConfig } from "../lib/forms/contact-form-config";
import { checkAuth } from "../lib/auth";
import { globalCompanyData } from "../pages/api/global/global-company-data";

const { currentUser } = await checkAuth(Astro.cookies);
const { globalCompanyName, virtualAssistantName } = await globalCompanyData();
const contactFormConfig = getContactFormConfig(globalCompanyName, virtualAssistantName);
---

<App title={`Contact ${globalCompanyName}`} {currentUser} mask="top-left">
  <div class="container mx-auto px-4 py-12">
    <!-- Choice Screen (Initial View) -->
    <div id="choice-screen" class="mx-auto max-w-2xl space-y-8 text-center">
      <div class="space-y-4">
        <h1 class="text-4xl font-bold text-gray-900 dark:text-white md:text-5xl">
          How would you like to get in touch?
        </h1>

        <p class="mx-auto max-w-xl text-lg text-gray-600 dark:text-gray-400">
          Choose your preferred method of contact. Both routes get you the same result!
        </p>
      </div>

      <div class="grid gap-6 md:grid-cols-2">
        <!-- Chat Option -->
        <button
          onclick="showChat()"
          class="group transform rounded-2xl bg-gradient-to-br from-primary-500 to-primary-600 p-8 text-left shadow-xl transition-all duration-300 hover:scale-105 hover:from-primary-600 hover:to-primary-700"
        >
          <div class="space-y-4 text-white">
            <div class="flex items-center justify-between">
              <svg class="h-16 w-16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"
                ></path>
              </svg>
              <span class="rounded-full bg-white/20 px-3 py-1 text-sm font-medium">
                Recommended
              </span>
            </div>

            <h2 class="text-2xl font-bold">Chat with Leah</h2>

            <p class="text-primary-100">
              Natural conversation with our AI assistant. Ask questions and get instant answers.
            </p>

            <ul class="space-y-2 text-sm text-primary-100">
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Voice or text - your choice</span>
              </li>
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Get instant answers to your questions</span>
              </li>
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Flexible, conversational flow</span>
              </li>
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Usually faster to complete</span>
              </li>
            </ul>

            <div
              class="mt-4 flex items-center justify-between border-t border-white/20 pt-4 text-sm"
            >
              <span class="font-medium">Start chatting →</span>
              <span class="opacity-75">~2 min</span>
            </div>
          </div>
        </button>

        <!-- Form Option -->
        <button
          onclick="showForm()"
          class="group transform rounded-2xl border-2 border-gray-200 bg-white p-8 text-left shadow-lg transition-all duration-300 hover:scale-105 hover:border-primary-500 dark:border-gray-700 dark:bg-gray-900 dark:hover:border-primary-500"
        >
          <div class="space-y-4">
            <div class="flex items-center justify-between">
              <svg
                class="h-16 w-16 text-gray-600 transition-colors group-hover:text-primary-600 dark:text-gray-400"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                ></path>
              </svg>
            </div>

            <h2 class="text-2xl font-bold text-gray-900 dark:text-white">
              Fill Out Traditional Form
            </h2>

            <p class="text-gray-600 dark:text-gray-400">
              Step-by-step guided form with visual confirmation at each stage.
            </p>

            <ul class="space-y-2 text-sm text-gray-600 dark:text-gray-400">
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Structured, labeled fields</span>
              </li>
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Visual progress tracking</span>
              </li>
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Traditional familiar format</span>
              </li>
              <li class="flex items-center gap-2">
                <svg
                  class="h-5 w-5 flex-shrink-0"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 13l4 4L19 7"></path>
                </svg>
                <span>No voice/audio required</span>
              </li>
            </ul>

            <div
              class="mt-4 flex items-center justify-between border-t border-gray-200 pt-4 text-sm text-gray-700 dark:border-gray-700 dark:text-gray-300"
            >
              <span class="font-medium">Start form →</span>
              <span class="opacity-75">~3 min</span>
            </div>
          </div>
        </button>
      </div>

      <!-- Feature Comparison -->
      <div class="mt-12 border-t border-gray-200 pt-8 dark:border-gray-700">
        <p class="text-center text-sm text-gray-500 dark:text-gray-400">
          Both methods save to the same database and get the same priority. Choose what's
          comfortable for you!
        </p>
      </div>
    </div>

    <!-- Chat Interface Container (Hidden Initially) -->
    <div id="chat-container" class="mx-auto hidden max-w-4xl">
      <button
        onclick="showChoice()"
        class="mb-6 flex items-center gap-2 font-medium text-primary-600 transition-colors hover:text-primary-700 dark:text-primary-400 dark:hover:text-primary-300"
      >
        <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span>Back to options</span>
      </button>

      <VapiChatInterface
        title="Let's get in touch..."
        introMessage={`Hi! I'm Leah, ${globalCompanyName}'s project assistant. I'm here to help you get in touch with our team. I can answer questions about our fire protection services and collect your contact information. What brings you here today?`}
        placeholder="Type your message or ask a question..."
      />

      <!-- Quick Examples -->
      <div
        class="mt-6 rounded-lg border border-blue-200 bg-blue-50 p-4 dark:border-blue-800 dark:bg-blue-900/20"
      >
        <p class="mb-2 text-sm font-medium text-blue-900 dark:text-blue-200">Try asking:</p>
        <div class="flex flex-wrap gap-2">
          <button
            onclick="document.getElementById('vapi-input').value = 'What services do you offer?'; document.getElementById('vapi-input').focus();"
            class="rounded-lg border border-blue-300 bg-white px-3 py-1 text-xs text-blue-800 transition-colors hover:bg-blue-50 dark:border-blue-700 dark:bg-gray-900 dark:text-blue-200 dark:hover:bg-blue-900/30"
          >
            What services do you offer?
          </button>
          <button
            onclick="document.getElementById('vapi-input').value = 'I need a fire alarm system'; document.getElementById('vapi-input').focus();"
            class="rounded-lg border border-blue-300 bg-white px-3 py-1 text-xs text-blue-800 transition-colors hover:bg-blue-50 dark:border-blue-700 dark:bg-gray-900 dark:text-blue-200 dark:hover:bg-blue-900/30"
          >
            I need a fire alarm system
          </button>
          <button
            onclick="document.getElementById('vapi-input').value = 'How long does a project take?'; document.getElementById('vapi-input').focus();"
            class="rounded-lg border border-blue-300 bg-white px-3 py-1 text-xs text-blue-800 transition-colors hover:bg-blue-50 dark:border-blue-700 dark:bg-gray-900 dark:text-blue-200 dark:hover:bg-blue-900/30"
          >
            How long does a project take?
          </button>
        </div>
      </div>
    </div>

    <!-- Form Container (Hidden Initially) -->
    <div id="form-container" class="hidden">
      <button
        onclick="showChoice()"
        class="mb-6 flex items-center gap-2 font-medium text-primary-600 transition-colors hover:text-primary-700 dark:text-primary-400 dark:hover:text-primary-300"
      >
        <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span>Back to options</span>
      </button>

      <MultiStepForm config={contactFormConfig} containerClass="w-full max-w-4xl mx-auto px-16" />
    </div>
  </div>
</App>

<script is:inline>
  // Navigation functions
  function showChat() {
    document.getElementById("choice-screen").classList.add("hidden");
    document.getElementById("chat-container").classList.remove("hidden");
    document.getElementById("form-container").classList.add("hidden");

    // Scroll to top smoothly
    window.scrollTo({ top: 0, behavior: "smooth" });

    // Focus the chat input after animation
    setTimeout(() => {
      const chatInput = document.getElementById("vapi-input");
      if (chatInput) chatInput.focus();
    }, 100);
  }

  function showForm() {
    document.getElementById("choice-screen").classList.add("hidden");
    document.getElementById("chat-container").classList.add("hidden");
    document.getElementById("form-container").classList.remove("hidden");

    // Scroll to top smoothly
    window.scrollTo({ top: 0, behavior: "smooth" });
  }

  function showChoice() {
    document.getElementById("choice-screen").classList.remove("hidden");
    document.getElementById("chat-container").classList.add("hidden");
    document.getElementById("form-container").classList.add("hidden");

    // Scroll to top smoothly
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
</script>
