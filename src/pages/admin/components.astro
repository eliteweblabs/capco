---
/**
 * Admin Components & Style Reference
 * Single reference for all interior page content styles used across admin.
 * Use this page when building new admin pages or matching existing patterns.
 *
 * Full component gallery (buttons, blocks, etc.): /admin/design
 * Placeholder patterns: /admin/design/placeholders
 */
import { checkAuth } from "../../lib/auth";
import LayoutFullWidth from "../../components/layouts/LayoutFullWidth.astro";
import Button from "../../components/common/Button.astro";
import Hero from "../../components/common/Hero.astro";
import DeleteConfirmButton from "../../components/ui/DeleteConfirmButton.astro";
import CloseButton from "../../components/ui/CloseButton.astro";
import InputLabelWrap from "../../components/common/__InputLabelWrap.astro";
import SlidingTabs from "../../components/common/SlidingTabs.astro";
import { globalClasses } from "../api/global/global-classes";
import SimpleIcon from "../../components/common/SimpleIcon.astro";

const { currentUser, session } = await checkAuth(Astro.cookies);
const currentRole = currentUser?.profile?.role;

if (!currentUser || !session) {
  return Astro.redirect("/auth/login");
}
if (currentRole !== "Admin") {
  return Astro.redirect("/dashboard");
}

const {
  globalInputClasses,
  globalAdminContainerClasses,
  primaryTextClasses,
  secondaryTextClasses,
  globalCodeInline,
} = globalClasses();
---

<LayoutFullWidth
  title="Components & Style Reference"
  classList="p-4"
  description="Admin interior page style reference. Use for any new admin work."
>
  <div class="px-4 py-8">
    <div class="mb-8">
      <h1 class={`mb-2 text-3xl font-bold ${primaryTextClasses}`}>Components & Style Reference</h1>
      <p class={`${secondaryTextClasses} text-gray-600 dark:text-gray-400`}>
        Patterns taken from real admin pages (Users, Settings, Notifications, Banner Alerts, CMS).
        Use when building new admin pages. Full gallery: <a
          href="/admin/design"
          class="text-primary-500 hover:underline">Design</a
        >. <a
          href="/admin/design/placeholders"
          class="text-primary-500 hover:underline">Placeholders</a
        >.
      </p>
    </div>

    <!-- ========== 1. Page structure ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">
        1. Page structure
      </h2>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-2 text-lg font-semibold`}>Wrapper</h3>
        <p class="mb-2 text-sm text-gray-600 dark:text-gray-400">
          Used on <a href="/admin/settings" class="text-primary-500 hover:underline">Settings</a>, <a
            href="/admin/cms"
            class="text-primary-500 hover:underline">CMS</a
          >, <a href="/admin/design" class="text-primary-500 hover:underline">Design</a>: <code
            class={globalCodeInline}>px-4 py-8</code> on the main content div.
        </p>
        <pre
          class="overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;div class="px-4 py-8"&gt;
  &lt;!-- page content --&gt;
&lt;/div&gt;</code></pre>
      </div>
    </section>

    <!-- ========== 2. Page headers ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">
        2. Page headers
      </h2>

      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white dark:border-gray-700 dark:bg-gray-900"
        data-accordion-card
      >
        <div class="flex items-center justify-between relative">
          <div
            class="flex-1 cursor-pointer rounded-lg p-4 transition-colors hover:bg-gray-50 dark:hover:bg-gray-700"
            data-accordion-target="#ref-header-simple"
            aria-expanded="false"
            aria-controls="ref-header-simple"
            role="button"
            tabindex="0"
          >
            <h3 class={`${primaryTextClasses} text-lg font-medium`}>
              Simple header (h1 + description)
            </h3>
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
              <a href="/admin/users" class="text-primary-500 hover:underline">Users</a>, <a
                href="/admin/settings"
                class="text-primary-500 hover:underline">Settings</a
              >, <a href="/admin/cms" class="text-primary-500 hover:underline">CMS</a>.
            </p>
          </div>
          <button
            class="absolute right-0 p-4 text-gray-400 transition-colors hover:text-gray-600 dark:hover:text-gray-300"
            data-accordion-target="#ref-header-simple"
            aria-expanded="false"
            type="button"
          >
            <svg
              class="h-4 w-4 transition-transform duration-200"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M19 9l-7 7-7-7"
              />
            </svg>
          </button>
        </div>
        <div
          id="ref-header-simple"
          class="hidden border-t border-gray-200 p-6 dark:border-gray-700"
        >
          <div class="mb-6">
            <h1 class={`${primaryTextClasses} mb-2 text-3xl font-bold`}>User Management</h1>
            <p class="text-gray-600 dark:text-gray-400">
              Create, view, and manage all system users
            </p>
          </div>
          <p class="mb-2 text-xs text-gray-500">From <code>AdminUsers.astro</code> / <code>pages/admin/settings.astro</code></p>
          <pre
            class="overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;h1 class={`mb-2 text-3xl font-bold ${'{'}primaryTextClasses{'}'}`}&gt;User Management&lt;/h1&gt;
&lt;p class={secondaryTextClasses}&gt;Create, view, and manage all system users&lt;/p&gt;</code></pre>
        </div>
      </div>

      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white dark:border-gray-700 dark:bg-gray-900"
        data-accordion-card
      >
        <div class="flex items-center justify-between relative">
          <div
            class="flex-1 cursor-pointer rounded-lg p-4 transition-colors hover:bg-gray-50 dark:hover:bg-gray-700"
            data-accordion-target="#ref-header-card"
            aria-expanded="false"
            aria-controls="ref-header-card"
            role="button"
            tabindex="0"
          >
            <h3 class={`${primaryTextClasses} text-lg font-medium`}>Card header (border-b)</h3>
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
              <a href="/admin/notifications" class="text-primary-500 hover:underline">Send Notifications</a> Create tab.
            </p>
          </div>
          <button
            class="absolute right-0 p-4 text-gray-400 transition-colors hover:text-gray-600 dark:hover:text-gray-300"
            data-accordion-target="#ref-header-card"
            aria-expanded="false"
            type="button"
          >
            <svg
              class="h-4 w-4 transition-transform duration-200"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M19 9l-7 7-7-7"
              />
            </svg>
          </button>
        </div>
        <div id="ref-header-card" class="hidden border-t border-gray-200 dark:border-gray-700">
          <div class="border-b border-gray-200 px-6 py-4 dark:border-gray-700">
            <h2 class={`text-lg font-medium ${primaryTextClasses}`}>Create Notification</h2>
          </div>
          <div class="p-6">
            <p class="text-sm text-gray-500">Form content (Recipients, Type, Title, Message).</p>
          </div>
          <p class="mx-6 mb-2 text-xs text-gray-500">From <code>pages/admin/notifications.astro</code></p>
          <pre
            class="mx-6 mb-6 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;div class="border-b border-gray-200 px-6 py-4 dark:border-gray-700"&gt;
  &lt;h2 class={`text-lg font-medium ${'{'}primaryTextClasses{'}'}`}&gt;Create Notification&lt;/h2&gt;
&lt;/div&gt;
&lt;form class="space-y-6 p-6"&gt;...&lt;/form&gt;</code></pre>
        </div>
      </div>

      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white dark:border-gray-700 dark:bg-gray-900"
        data-accordion-card
      >
        <div class="flex items-center justify-between relative">
          <div
            class="flex-1 cursor-pointer rounded-lg p-4 transition-colors hover:bg-gray-50 dark:hover:bg-gray-700"
            data-accordion-target="#ref-header-hero"
            aria-expanded="false"
            aria-controls="ref-header-hero"
            role="button"
            tabindex="0"
          >
            <h3 class={`${primaryTextClasses} text-lg font-medium`}>Hero header</h3>
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
              <a href="/admin/banner-alerts" class="text-primary-500 hover:underline">Banner Alerts</a>, <a
                href="/admin/notifications"
                class="text-primary-500 hover:underline">Notifications</a
              >, <a href="/admin/media" class="text-primary-500 hover:underline">Media</a>.
            </p>
          </div>
          <button
            class="absolute right-0 p-4 text-gray-400 transition-colors hover:text-gray-600 dark:hover:text-gray-300"
            data-accordion-target="#ref-header-hero"
            aria-expanded="false"
            type="button"
          >
            <svg
              class="h-4 w-4 transition-transform duration-200"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M19 9l-7 7-7-7"
              />
            </svg>
          </button>
        </div>
        <div id="ref-header-hero" class="hidden border-t border-gray-200 dark:border-gray-700">
          <div class="p-6">
            <Hero
              title="Banner Alerts"
              description="Manage site-wide banner alerts that appear at the top or bottom of the page"
              {primaryTextClasses}
              {secondaryTextClasses}
            />
          </div>
          <p class="mx-6 mb-2 text-xs text-gray-500">From <code>pages/admin/banner-alerts.astro</code></p>
          <pre
            class="mx-6 mb-6 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;Hero
  title="Banner Alerts"
  description="Manage site-wide banner alerts..."
  {'{'}primaryTextClasses{'}'}
  {'{'}secondaryTextClasses{'}'}
/&gt;</code></pre>
        </div>
      </div>
    </section>

    <!-- ========== 3. Containers ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">3. Containers</h2>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-2 text-lg font-semibold`}>globalAdminContainerClasses</h3>
        <p class="mb-3 text-sm text-gray-600 dark:text-gray-400">
          From <code class={globalCodeInline}>global-classes.ts</code>. Used for
          <a href="/admin/banner-alerts" class="text-primary-500 hover:underline">Banner Alerts</a> “Existing Banners” tab panel, <a
            href="/admin/notifications"
            class="text-primary-500 hover:underline">Notifications</a
          > create panel, and stat cards in Users/Discussions/Activity.
        </p>
        <div class={globalAdminContainerClasses}>
          <div class="border-b border-gray-200 px-6 py-4 dark:border-gray-700">
            <h4 class={`${primaryTextClasses} font-semibold`}>Existing Banners</h4>
          </div>
          <div class="p-6">
            <p class="text-sm text-gray-500">Banner list content. Uses color-background, rounded-lg, shadow.</p>
          </div>
        </div>
        <pre
          class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>const {'{'} globalAdminContainerClasses {'}'} = globalClasses();
&lt;div class={'{'}globalAdminContainerClasses{'}'}&gt;...&lt;/div&gt;</code></pre>
      </div>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-2 text-lg font-semibold`}>
          Bordered card (accordion-style)
        </h3>
        <p class="mb-3 text-sm text-gray-600 dark:text-gray-400">
          Section cards on Settings, Design: <code class={globalCodeInline}
            >rounded-lg border border-gray-200 bg-white dark:border-gray-700 dark:bg-gray-900</code
          >
        </p>
        <div
          class="rounded-lg border border-gray-200 bg-white p-4 dark:border-gray-700 dark:bg-gray-900"
        >
          <p class="text-sm text-gray-500">Section content.</p>
        </div>
      </div>
    </section>

    <!-- ========== 4. Messages / Alerts ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">
        4. Messages &amp; alerts
      </h2>
      <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
        Used on <a href="/admin/settings" class="text-primary-500 hover:underline">Settings</a> (success-message, error-message), info boxes, banner list active/inactive.
      </p>
      <div class="space-y-4">
        <div
          class="rounded-lg border border-green-200 bg-green-50 p-4 dark:border-green-800 dark:bg-green-900/20"
        >
          <p class="text-sm text-green-800 dark:text-green-200">Success message.</p>
        </div>
        <div
          class="rounded-lg border border-red-200 bg-red-50 p-4 dark:border-red-800 dark:bg-red-900/20"
        >
          <p class="text-sm text-red-800 dark:text-red-200">Error message.</p>
        </div>
        <div
          class="dark:bg-primary-900/20 rounded-lg border border-primary-200 bg-primary-50 p-4 dark:border-primary-800"
        >
          <p class="text-sm text-primary-800 dark:text-primary-200">Info / primary message.</p>
        </div>
        <div
          class="rounded-lg border border-amber-200 bg-amber-50 p-4 dark:border-amber-800 dark:bg-amber-950/30"
        >
          <p class="text-sm text-amber-800 dark:text-amber-200">Warning message.</p>
        </div>
      </div>
      <pre
        class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>{/* Success */}
&lt;div class="rounded-lg border border-green-200 bg-green-50 p-4 dark:border-green-800 dark:bg-green-900/20"&gt;
  &lt;p class="text-sm text-green-800 dark:text-green-200"&gt;...&lt;/p&gt;
&lt;/div&gt;
{/* Error: red-200/red-50, dark:red-800/red-900/20 */}</code></pre>
    </section>

    <!-- ========== 5. Form elements ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">
        5. Form elements
      </h2>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-3 text-lg font-semibold`}>
          globalInputClasses + InputLabelWrap
        </h3>
        <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
          Used on <a href="/admin/settings" class="text-primary-500 hover:underline">Settings</a> (Company Name, Slogan, Address…), <a
            href="/admin/notifications"
            class="text-primary-500 hover:underline">Notifications</a
          > (Recipients, Title, Message). <code class={globalCodeInline}>globalInputClasses</code> from global-classes; <code>InputLabelWrap</code> or manual <code>label</code>.
        </p>
        <div class="max-w-md space-y-4">
          <InputLabelWrap id="ref-input-1" label="Title *" show={true}>
            <input
              type="text"
              id="ref-input-1"
              class={globalInputClasses}
              placeholder="Notification title"
            />
          </InputLabelWrap>
          <InputLabelWrap id="ref-input-2" label="Company Name" required show={true}>
            <input type="text" id="ref-input-2" class={globalInputClasses} placeholder="Your Company Name" />
          </InputLabelWrap>
        </div>
        <pre
          class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>const {'{'} globalInputClasses {'}'} = globalClasses();
&lt;label for="title" class="mb-2 block text-sm font-medium"&gt;Title *&lt;/label&gt;
&lt;input id="title" name="title" class={'{'}globalInputClasses{'}'} placeholder="Notification title" /&gt;</code></pre>
      </div>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-3 text-lg font-semibold`}>
          Textarea (standard)
        </h3>
        <p class="mb-3 text-sm text-gray-600 dark:text-gray-400">
          Same <code class={globalCodeInline}>globalInputClasses</code>.
          Use <code>resize-y</code> if you want vertical resize only.
        </p>
        <div class="max-w-md">
          <InputLabelWrap id="ref-textarea-1" label="Message" show={true}>
            <textarea
              id="ref-textarea-1"
              name="message"
              rows={4}
              class={`${globalInputClasses} resize-y`}
              placeholder="Enter text..."
            />
          </InputLabelWrap>
        </div>
        <pre
          class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;textarea id="..." class="{'{'}globalInputClasses{'}'} resize-y" rows={4} /&gt;</code></pre>
      </div>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-3 text-lg font-semibold`}>
          Textarea (expanding)
        </h3>
        <p class="mb-3 text-sm text-gray-600 dark:text-gray-400">
          Add class <code class={globalCodeInline}>expanding-textarea</code>
          so the field grows with content (CSS <code>field-sizing: content</code>). Used in
          MultiStepForm when field config has <code>"classes": "expanding-textarea"</code>.
        </p>
        <div class="max-w-md">
          <InputLabelWrap id="ref-textarea-expanding" label="Expanding (type to test)" show={true}>
            <textarea
              id="ref-textarea-expanding"
              name="message-expanding"
              rows={1}
              class={`${globalInputClasses} expanding-textarea`}
              placeholder="Type here — field expands as you fill..."
            />
          </InputLabelWrap>
        </div>
        <pre
          class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;textarea class="{'{'}globalInputClasses{'}'} expanding-textarea" rows={1} /&gt;</code></pre>
      </div>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-3 text-lg font-semibold`}>
          Select
        </h3>
        <p class="mb-3 text-sm text-gray-600 dark:text-gray-400">
          Notifications: Recipients, Type, Priority. Banner Alerts: Type (info/success/warning/error), Position (top/bottom). <code class={globalCodeInline}>globalInputClasses</code> on <code>&lt;select&gt;</code>.
        </p>
        <div class="max-w-md">
          <InputLabelWrap id="ref-select-1" label="Type" show={true}>
            <select id="ref-select-1" name="type" class={globalInputClasses}>
              <option value="info">Info</option>
              <option value="success">Success</option>
              <option value="warning">Warning</option>
              <option value="error">Error</option>
            </select>
          </InputLabelWrap>
        </div>
        <pre
          class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;select id="type" name="type" class={'{'}globalInputClasses{'}'}&gt;
  &lt;option value="info"&gt;Info&lt;/option&gt;...
&lt;/select&gt;</code></pre>
      </div>
      <div
        class="mb-4 rounded-lg border border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <h3 class={`${primaryTextClasses} mb-3 text-lg font-semibold`}>
          Toggle button group (single-select)
        </h3>
        <p class="mb-3 text-sm text-gray-600 dark:text-gray-400">
          <code>variant="selected"</code> for active, <code>variant="outline"</code> for inactive. Wire with JS.
        </p>
        <div class="flex flex-wrap gap-2">
          <Button type="button" variant="selected" class="text-md">Option A</Button>
          <Button type="button" variant="outline" class="text-md">Option B</Button>
          <Button type="button" variant="outline" class="text-md">Option C</Button>
        </div>
      </div>
    </section>

    <!-- ========== 6. Buttons ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">6. Buttons</h2>
      <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
        <code>Button.astro</code> everywhere. <code>DeleteConfirmButton.astro</code> in <a
          href="/admin/cms"
          class="text-primary-500 hover:underline">CMS</a
        > row actions (<code>CmsPageRowActions</code>), <a
          href="/admin/banner-alerts"
          class="text-primary-500 hover:underline">Banner Alerts</a
        > edit/delete, Users. <code>CloseButton.astro</code> for modals/drawers. Gallery: <a
          href="/admin/design"
          class="text-primary-500 hover:underline">Design</a
        >.
      </p>
      <div class="mb-4 flex flex-wrap gap-3">
        <Button variant="primary">Primary</Button>
        <Button variant="secondary">Secondary</Button>
        <Button variant="outline">Outline</Button>
        <Button variant="danger">Danger</Button>
        <Button variant="success">Success</Button>
      </div>
      <p class="mb-2 text-sm text-gray-600 dark:text-gray-400">
        DeleteConfirmButton (2-step confirm) in each size:
      </p>
      <div class="flex flex-wrap items-center gap-6">
        <div class="flex items-center gap-2">
          <DeleteConfirmButton id="ref-delete-demo-xs" size="xs" />
          <span class="text-xs text-gray-500">xs</span>
        </div>
        <div class="flex items-center gap-2">
          <DeleteConfirmButton id="ref-delete-demo-sm" size="sm" />
          <span class="text-xs text-gray-500">sm</span>
        </div>
        <div class="flex items-center gap-2">
          <DeleteConfirmButton id="ref-delete-demo-md" size="md" />
          <span class="text-xs text-gray-500">md</span>
        </div>
        <div class="flex items-center gap-2">
          <DeleteConfirmButton id="ref-delete-demo-lg" size="lg" />
          <span class="text-xs text-gray-500">lg</span>
        </div>
        <div class="flex items-center gap-2">
          <DeleteConfirmButton id="ref-delete-demo-xl" size="xl" />
          <span class="text-xs text-gray-500">xl</span>
        </div>
      </div>
      <div class="mt-3 flex flex-wrap items-center gap-4">
        <CloseButton tooltipText="Close" />
        <span class="text-sm text-gray-500">CloseButton (for modals/drawers)</span>
      </div>
    </section>

    <!-- ========== 7. Tabs ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">7. Tabs</h2>
      <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
        <a href="/admin/notifications" class="text-primary-500 hover:underline">Notifications</a>: Create | Archive. <a
          href="/admin/banner-alerts"
          class="text-primary-500 hover:underline">Banner Alerts</a
        >: Existing Banners | Create New Banner. <code>SlidingTabs.astro</code> with <code>variant: "tab"</code>; panels use <code>activeItem</code> or URL <code>tab</code> param.
      </p>
      <SlidingTabs
        navId="ref-tabs-demo"
        activeItem={0}
        urlParam="tab"
        items={[
          { id: "tab-create", label: "Create", variant: "tab", showLabel: true },
          { id: "tab-archive", label: "Archive", variant: "tab", showLabel: true },
        ]}
      />
      <div
        class="rounded-b-lg border border-t-0 border-gray-200 bg-white p-6 dark:border-gray-700 dark:bg-gray-900"
      >
        <p class="text-sm text-gray-500">Tab panel content. Hide/show by tab index or URL param (e.g. <code>?tab=archive</code>).</p>
      </div>
    </section>

    <!-- ========== 8. Badges / tags ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">
        8. Badges &amp; tags
      </h2>
      <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
        <a href="/admin/banner-alerts" class="text-primary-500 hover:underline">Banner Alerts</a> list: type (info/success/warning/error), position (top/bottom), “Inactive”, expire time.
      </p>
      <div class="flex flex-wrap gap-2">
        <span
          class="inline-flex items-center rounded bg-blue-100 px-2 py-0.5 text-xs font-medium text-blue-800 dark:bg-blue-900/50 dark:text-blue-300"
          >info</span
        >
        <span
          class="inline-flex items-center rounded bg-green-100 px-2 py-0.5 text-xs font-medium text-green-800 dark:bg-green-900/50 dark:text-green-300"
          >success</span
        >
        <span
          class="inline-flex items-center rounded bg-orange-100 px-2 py-0.5 text-xs font-medium text-orange-800 dark:bg-orange-900/50 dark:text-orange-300"
          >warning</span
        >
        <span
          class="inline-flex items-center rounded bg-red-100 px-2 py-0.5 text-xs font-medium text-red-800 dark:bg-red-900/50 dark:text-red-300"
          >error</span
        >
        <span
          class="inline-flex items-center rounded bg-gray-100 px-2 py-0.5 text-xs font-medium text-gray-800 dark:bg-gray-700 dark:text-gray-300"
          >neutral</span
        >
      </div>
      <pre
        class="mt-4 overflow-x-auto rounded bg-gray-100 p-3 text-xs dark:bg-gray-800"><code>&lt;span class="inline-flex items-center rounded px-2 py-0.5 text-xs font-medium bg-green-100 text-green-800 dark:bg-green-900/50 dark:text-green-300"&gt;success&lt;/span&gt;</code></pre>
    </section>

    <!-- ========== 9. Tables ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">9. Tables</h2>
      <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
        <a href="/admin/cms" class="text-primary-500 hover:underline">CMS</a> page list and <a
          href="/admin/users"
          class="text-primary-500 hover:underline">Users</a
        > list use <code>AccordionDataTable.astro</code> (expandable rows, drag, actions). Table styles come from <code>global-classes.ts</code> (<code>dataTableCardClasses</code>, <code>dataTableHeaderBarClasses</code>, etc.). Simple tables: same wrapper classes.
      </p>
      <div
        class="overflow-x-auto rounded-lg border border-gray-200 bg-white shadow dark:border-gray-700 dark:bg-gray-900"
      >
        <table class="w-full text-left text-sm text-gray-500 dark:text-gray-400">
          <thead
            class="bg-gray-50 text-xs uppercase text-gray-700 dark:bg-gray-700 dark:text-gray-300"
          >
            <tr>
              <th class="px-6 py-3">Slug</th>
              <th class="px-6 py-3">Title</th>
              <th class="px-6 py-3">Actions</th>
            </tr>
          </thead>
          <tbody>
            <tr class="border-b border-gray-200 dark:border-gray-700">
              <td class="px-6 py-4 font-medium">/about</td>
              <td class="px-6 py-4">About Us</td>
              <td class="px-6 py-4"><Button variant="ghost" size="xs">Edit</Button></td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- ========== 10. List items (card rows) ========== -->
    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold text-primary-600 dark:text-primary-400">
        10. List items (card rows)
      </h2>
      <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">
        <a href="/admin/banner-alerts" class="text-primary-500 hover:underline">Banner Alerts</a> list: each banner is a card row; green border when active, gray when inactive; type and position badges.
      </p>
      <div class="space-y-4">
        <div
          class="rounded-lg border border-green-300 bg-green-50 p-4 dark:border-green-700 dark:bg-green-900/20"
        >
          <div class="flex items-center justify-between gap-2 flex-wrap">
            <span class={`${primaryTextClasses} font-medium`}>Site maintenance notice</span>
            <span
              class="inline-flex items-center rounded bg-green-100 px-2 py-0.5 text-xs font-medium text-green-800 dark:bg-green-900/50 dark:text-green-300"
              >active</span
            >
            <span
              class="inline-flex items-center rounded bg-blue-100 px-2 py-0.5 text-xs font-medium text-blue-800 dark:bg-blue-900/50 dark:text-blue-300"
              >info</span
            >
          </div>
          <p class="mt-1 text-sm text-gray-600 dark:text-gray-400">We'll be back shortly. Estimated 1 hour.</p>
        </div>
        <div
          class="rounded-lg border border-gray-300 bg-gray-50 p-4 dark:border-gray-600 dark:bg-gray-700/50"
        >
          <div class="flex items-center justify-between gap-2 flex-wrap">
            <span class={`${primaryTextClasses} font-medium`}>Old promo (disabled)</span>
            <span
              class="rounded bg-gray-200 px-2 py-0.5 text-xs font-medium text-gray-700 dark:bg-gray-600 dark:text-gray-300"
              >Inactive</span
            >
          </div>
        </div>
      </div>
    </section>

    <!-- ========== Quick links ========== -->
    <section
      class="dark:bg-primary-900/20 rounded-lg border border-primary-200 bg-primary-50 p-6 dark:border-primary-800"
    >
      <h2 class="mb-3 text-lg font-semibold text-primary-800 dark:text-primary-200">Quick links</h2>
      <ul class="list-inside list-disc space-y-1 text-sm text-primary-700 dark:text-primary-300">
        <li><a href="/admin/users" class="hover:underline">Users</a> – User Management (AdminUsers)</li>
        <li><a href="/admin/settings" class="hover:underline">Settings</a> – Global Settings (forms, messages)</li>
        <li><a href="/admin/notifications" class="hover:underline">Notifications</a> – Send Notifications (tabs, form)</li>
        <li><a href="/admin/banner-alerts" class="hover:underline">Banner Alerts</a> – Hero, tabs, list items, badges</li>
        <li><a href="/admin/cms" class="hover:underline">CMS</a> – Page list (AccordionDataTable)</li>
        <li><a href="/admin/media" class="hover:underline">Media</a> – Media Manager (Hero, grid)</li>
        <li><a href="/admin/design" class="hover:underline">Design</a> – Full component gallery</li>
        <li><a href="/admin/design/placeholders" class="hover:underline">Placeholders</a> – MultiStepForm patterns</li>
        <li><a href="/admin/icons" class="hover:underline">Icons</a> – SimpleIcon / BoxIcons</li>
        <li><code>src/pages/api/global/global-classes.ts</code> – globalInputClasses, globalAdminContainerClasses, dataTableCardClasses, primaryTextClasses, secondaryTextClasses</li>
        <li><code>src/lib/button-styles.ts</code> – Button variants and getButtonClasses()</li>
      </ul>
    </section>
  </div>
</LayoutFullWidth>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const accordionButtons = document.querySelectorAll("[data-accordion-target]");
    accordionButtons.forEach((button) => {
      button.addEventListener("click", function (this: HTMLElement) {
        const targetId = this.getAttribute("data-accordion-target");
        const target = document.querySelector(targetId as string);
        const isExpanded = this.getAttribute("aria-expanded") === "true";
        if (target) {
          if (isExpanded) {
            target.classList.add("hidden");
            this.setAttribute("aria-expanded", "false");
          } else {
            target.classList.remove("hidden");
            this.setAttribute("aria-expanded", "true");
          }
          const arrow = this.querySelector("svg");
          if (arrow) {
            (arrow as Element).classList.toggle("rotate-180");
          }
          const parent = this.closest("[data-accordion-card]");
          if (parent) {
            const allToggles = parent.querySelectorAll(`[data-accordion-target="${targetId}"]`);
            allToggles.forEach((toggle) => {
              if (toggle !== this) {
                toggle.setAttribute("aria-expanded", String(!isExpanded));
                const toggleArrow = toggle.querySelector("svg");
                if (toggleArrow) {
                  (toggleArrow as Element).classList.toggle("rotate-180");
                }
              }
            });
          }
        }
      });
    });
  });
</script>
