---
import { checkAuth } from "../../../lib/auth";
import LayoutFullWidth from "../../../components/layouts/LayoutFullWidth.astro";
import LoadingSpinner from "../../../components/ui/LoadingSpinner.astro";
import { globalClasses } from "../../api/global/global-classes";

const { currentUser, session } = await checkAuth(Astro.cookies);
if (!currentUser || !session || currentUser?.profile?.role !== "Admin") {
  return Astro.redirect("/auth/login");
}
const { primaryTextClasses, mutedTextClasses, globalCardStyle } = globalClasses();
---

<LayoutFullWidth title="Loading Spinner" description="Loading indicators with different sizes and colors." classList="p-4">
  <div class="px-4 py-8">
    <h1 class={`mb-2 text-3xl font-bold ${primaryTextClasses}`}>Loading Spinner</h1>
    <p class={`mb-6 ${mutedTextClasses}`}>Loading indicators with different sizes and colors.</p>

    <div class={`flex flex-wrap items-start gap-8 ${globalCardStyle} p-6`}>
      <div class="text-center">
        <p class="mb-2 text-sm text-gray-500">Small / Primary</p>
        <LoadingSpinner size="sm" color="primary" loadingText="Loading..." />
      </div>
      <div class="text-center">
        <p class="mb-2 text-sm text-gray-500">Medium / Success</p>
        <LoadingSpinner size="md" color="success" loadingText="Processing..." />
      </div>
      <div class="text-center">
        <p class="mb-2 text-sm text-gray-500">Large / Danger</p>
        <LoadingSpinner size="lg" color="danger" loadingText="Please wait..." />
      </div>
      <div class="text-center">
        <p class="mb-2 text-sm text-gray-500">Warning</p>
        <LoadingSpinner size="md" color="warning" loadingText="Saving..." />
      </div>
    </div>
  </div>
</LayoutFullWidth>
