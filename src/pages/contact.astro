---
/**
 * Contact page - redirects to contact-hybrid (working form with typewriter).
 * The CMS contact page at /contact has form init/typewriter issues when embedded.
 */
return Astro.redirect("/contact-hybrid");
---
