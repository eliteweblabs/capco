---
/**
 * Test page: layout="fullscreen"
 * Page is min-h-dvh; main content scrolls, footer stays at bottom. Good for long forms/dashboards.
 */
import App from "../../components/ui/App.astro";
import { checkAuth } from "../../lib/auth";
import { globalClasses } from "../api/global/global-classes";

const { currentUser } = await checkAuth(Astro.cookies);
const { globalCardStyle } = globalClasses();
---

<App
  title="Layout: Fullscreen"
  description="Test page for fullscreen template"
  layout="fullscreen"
  {currentUser}
>
  <div class="flex min-h-0 flex-col px-4 py-8 sm:px-8">
    <h1 class="mb-2 text-2xl font-bold text-gray-900 dark:text-white">Fullscreen layout</h1>
    <p class="mb-8 text-gray-600 dark:text-gray-400">
      This page uses <code class="rounded bg-gray-200 px-1 dark:bg-gray-700"
        >layout="fullscreen"</code
      >. The viewport is at least 100dvh; the main area scrolls and the footer stays pinned at the
      bottom. Add enough content below to scroll and verify the footer behavior.
    </p>

    <div class="mb-10 space-y-6">
      <section class={`${globalCardStyle} p-4`}>
        <h2 class="mb-2 font-semibold text-gray-900 dark:text-white">Section 1</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt.
        </p>
      </section>
      <section class={`${globalCardStyle} p-4`}>
        <h2 class="mb-2 font-semibold text-gray-900 dark:text-white">Section 2</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip.
        </p>
      </section>
      <section class={`${globalCardStyle} p-4`}>
        <h2 class="mb-2 font-semibold text-gray-900 dark:text-white">Section 3</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore.
        </p>
      </section>
      <section class={`${globalCardStyle} p-4`}>
        <h2 class="mb-2 font-semibold text-gray-900 dark:text-white">Section 4</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt.
        </p>
      </section>
      <section class={`${globalCardStyle} p-4`}>
        <h2 class="mb-2 font-semibold text-gray-900 dark:text-white">
          Section 5 (scroll to see footer)
        </h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          When you scroll down, the footer should remain at the bottom of the viewport. This block
          adds enough height to trigger scrolling on most screens.
        </p>
      </section>
    </div>
  </div>
</App>
