---
/**
 * Test page: layout="fullscreen"
 * Page is min-h-dvh; main content scrolls, footer stays at bottom. Good for long forms/dashboards.
 */
import App from "../../components/ui/App.astro";
import { checkAuth } from "../../lib/auth";

const { currentUser } = await checkAuth(Astro.cookies);
---

<App title="Layout: Fullscreen" description="Test page for fullscreen template" layout="fullscreen" {currentUser}>
  <div class="flex flex-col min-h-0 px-4 sm:px-8 py-8">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">Fullscreen layout</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-8">
      This page uses <code class="rounded bg-gray-200 dark:bg-gray-700 px-1">layout="fullscreen"</code>.
      The viewport is at least 100dvh; the main area scrolls and the footer stays pinned at the
      bottom. Add enough content below to scroll and verify the footer behavior.
    </p>

    <div class="space-y-6 mb-10">
      <section class="rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="font-semibold text-gray-900 dark:text-white mb-2">Section 1</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt.
        </p>
      </section>
      <section class="rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="font-semibold text-gray-900 dark:text-white mb-2">Section 2</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip.
        </p>
      </section>
      <section class="rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="font-semibold text-gray-900 dark:text-white mb-2">Section 3</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore.
        </p>
      </section>
      <section class="rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="font-semibold text-gray-900 dark:text-white mb-2">Section 4</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt.
        </p>
      </section>
      <section class="rounded-lg border border-gray-200 dark:border-gray-700 p-4">
        <h2 class="font-semibold text-gray-900 dark:text-white mb-2">Section 5 (scroll to see footer)</h2>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          When you scroll down, the footer should remain at the bottom of the viewport. This block
          adds enough height to trigger scrolling on most screens.
        </p>
      </section>
    </div>
  </div>
</App>
