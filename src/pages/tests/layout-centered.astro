---
/**
 * Test page: layout="centered"
 * Content centered vertically and horizontally (e.g. login, 404, splash).
 */
import App from "../../components/ui/App.astro";
import { checkAuth } from "../../lib/auth";

const { currentUser } = await checkAuth(Astro.cookies);
---

<App
  title="Layout: Centered"
  description="Test page for centered template"
  layout="centered"
  mask="center"
  {currentUser}
>
  <div class="px-6 text-center">
    <h1 class="mb-3 text-2xl font-bold text-gray-900 dark:text-white">Centered layout</h1>
    <p class="mx-auto mb-6 max-w-md text-gray-600 dark:text-gray-400">
      This page uses <code class="rounded bg-gray-200 px-1 dark:bg-gray-700">layout="centered"</code
      >. The main content is centered both vertically and horizontally—typical for login, 404, or
      single-message pages.
    </p>

    <div
      class="inline-flex max-w-sm flex-col gap-3 rounded-lg border border-gray-200 bg-gray-50 p-6 text-left dark:border-gray-700 dark:bg-gray-800/50"
    >
      <p class="font-medium text-gray-900 dark:text-white">Dummy card</p>
      <p class="text-sm text-gray-600 dark:text-gray-400">
        A small block to verify centering and padding. Resize the window to see the content stay
        centered.
      </p>
    </div>
  </div>
</App>
