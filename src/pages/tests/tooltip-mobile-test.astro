---
import LayoutDefault from "../../components/layouts/LayoutDefault.astro";
import Tooltip from "../../components/common/TooltipFloating.astro";
import SimpleIcon from "../../components/common/SimpleIcon.astro";
---

<LayoutDefault
  title="Tooltip Mobile Test"
  description="Test page for mobile clickable tooltip functionality"
>
  <div class="container mx-auto space-y-12 p-8">
    <h1 class="mb-8 text-3xl font-bold">Tooltip Mobile Clickable Test</h1>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold">Standard Tooltip (Hover Only)</h2>
      <p class="text-gray-600 dark:text-gray-400">
        This tooltip only works on hover - won't work on mobile touch devices
      </p>
      <div class="flex items-center gap-4">
        <Tooltip text="This only shows on hover" position="top">
          <span class="rounded border bg-gray-100 px-4 py-2 dark:bg-gray-800">Hover Me</span>
        </Tooltip>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold">Always Open Tooltip</h2>
      <p class="text-gray-600 dark:text-gray-400">
        This tooltip is always visible on both desktop and mobile
      </p>
      <div class="flex items-center gap-4">
        <Tooltip text="Always visible tooltip" position="top" open={true}>
          <span class="rounded border bg-blue-100 px-4 py-2 dark:bg-blue-900">Always Visible</span>
        </Tooltip>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold">Mobile Clickable Tooltip (NEW)</h2>
      <p class="text-gray-600 dark:text-gray-400">
        This tooltip shows on hover (desktop) or tap (mobile)
      </p>
      <div class="flex flex-wrap items-center gap-4">
        <Tooltip text="Tap me on mobile!" position="top" mobileClickable={true}>
          <span class="cursor-pointer rounded border bg-green-100 px-4 py-2 dark:bg-green-900"
            >Tap/Hover Me</span
          >
        </Tooltip>

        <Tooltip text="Info icon with tooltip" position="right" mobileClickable={true}>
          <span class="cursor-pointer">
            <SimpleIcon name="info-circle" size="xl" />
          </span>
        </Tooltip>

        <Tooltip text="Help icon tooltip" position="bottom" mobileClickable={true}>
          <span class="cursor-pointer">
            <SimpleIcon name="help-circle" size="xl" />
          </span>
        </Tooltip>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold">Multiple Positions</h2>
      <p class="text-gray-600 dark:text-gray-400">
        Test mobile clickable tooltips in different positions
      </p>
      <div class="flex flex-wrap items-center justify-center gap-8 py-12">
        <Tooltip text="Top tooltip" position="top" mobileClickable={true}>
          <span class="cursor-pointer rounded border bg-purple-100 px-4 py-2 dark:bg-purple-900"
            >Top</span
          >
        </Tooltip>

        <Tooltip text="Bottom tooltip" position="bottom" mobileClickable={true}>
          <span class="cursor-pointer rounded border bg-purple-100 px-4 py-2 dark:bg-purple-900"
            >Bottom</span
          >
        </Tooltip>

        <Tooltip text="Left tooltip" position="left" mobileClickable={true}>
          <span class="cursor-pointer rounded border bg-purple-100 px-4 py-2 dark:bg-purple-900"
            >Left</span
          >
        </Tooltip>

        <Tooltip text="Right tooltip" position="right" mobileClickable={true}>
          <span class="cursor-pointer rounded border bg-purple-100 px-4 py-2 dark:bg-purple-900"
            >Right</span
          >
        </Tooltip>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold">With Buttons (Don't Use mobileClickable)</h2>
      <p class="text-gray-600 dark:text-gray-400">
        For buttons and links, use open={true} or standard hover since they handle their own click events
      </p>
      <div class="flex items-center gap-4">
        <Tooltip text="Button tooltip" position="top" open={true}>
          <button class="rounded border bg-blue-500 px-4 py-2 text-white hover:bg-blue-600">
            Click Button
          </button>
        </Tooltip>

        <Tooltip text="Link tooltip" position="top" open={true}>
          <a
            href="#"
            class="inline-block rounded border bg-blue-500 px-4 py-2 text-white hover:bg-blue-600"
          >
            Click Link
          </a>
        </Tooltip>
      </div>
    </section>

    <section class="space-y-4">
      <h2 class="text-2xl font-semibold">Non-Interactive Elements</h2>
      <p class="text-gray-600 dark:text-gray-400">
        Best use case: tooltips on spans, divs, and icons that don't trigger navigation
      </p>
      <div class="space-y-4">
        <p>
          This is a paragraph with
          <Tooltip text="Inline tooltip explanation" position="top" mobileClickable={true}>
            <span
              class="cursor-pointer text-blue-600 underline decoration-dotted dark:text-blue-400"
              >a term that needs explanation</span
            >
          </Tooltip>
          in the middle of the text.
        </p>

        <div class="flex items-center gap-2">
          <span>Device Status:</span>
          <Tooltip text="System is operating normally" position="right" mobileClickable={true}>
            <span class="cursor-pointer text-green-500">● Online</span>
          </Tooltip>
        </div>
      </div>
    </section>

    <section class="space-y-4 rounded-lg bg-gray-100 p-6 dark:bg-gray-800">
      <h2 class="text-2xl font-semibold">How to Test</h2>
      <ul class="list-inside list-disc space-y-2 text-gray-700 dark:text-gray-300">
        <li>
          <strong>On Desktop:</strong> Hover over any element - standard and mobile clickable tooltips
          work the same
        </li>
        <li>
          <strong>On Mobile:</strong>
          <ul class="ml-6 mt-2 list-inside list-disc space-y-1">
            <li>Standard tooltips won't appear</li>
            <li>Always open tooltips stay visible</li>
            <li>Mobile clickable tooltips toggle when you tap</li>
            <li>Tap outside to close mobile clickable tooltips</li>
          </ul>
        </li>
      </ul>
    </section>
  </div>
</LayoutDefault>
