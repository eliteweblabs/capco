---
/**
 * Test page: layout="minimal"
 * Minimal layout—full app chrome but layout value is "minimal" for data-template / CMS.
 */
import App from "../../components/ui/App.astro";
import { checkAuth } from "../../lib/auth";

const { currentUser } = await checkAuth(Astro.cookies);
---

<App title="Layout: Minimal" description="Test page for minimal template" layout="minimal" {currentUser}>
  <div class="container mx-auto max-w-4xl px-4 py-8">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">Minimal layout</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6">
      This page uses <code class="rounded bg-gray-200 dark:bg-gray-700 px-1">layout="minimal"</code>.
      You still get header and footer; the main difference is the template identifier for CMS or
      theming (data-template).
    </p>

    <ul class="list-disc pl-6 text-gray-700 dark:text-gray-300 space-y-2 mb-8">
      <li>First bullet for dummy content</li>
      <li>Second bullet</li>
      <li>Third bullet</li>
    </ul>

    <div class="rounded-lg border border-gray-200 dark:border-gray-700 p-4 bg-gray-50 dark:bg-gray-800/50">
      <p class="text-sm text-gray-600 dark:text-gray-400">
        Use this page to verify minimal template behavior and that content is constrained to a
        readable width.
      </p>
    </div>
  </div>
</App>
