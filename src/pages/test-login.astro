---
/**
 * Test login: redirects to standalone Google sign-in page.
 * For full login form without App, use /auth/login or /auth/google.
 */
const redirect = Astro.url.searchParams.get("redirect") || "/project/dashboard";
return Astro.redirect(`/auth/google?redirect=${encodeURIComponent(redirect)}`);
---
