---
import PushNotificationDemo from "@/components/common/PushNotificationDemo.astro";
import App from "../components/common/App.astro";
import SectionContainer from "../components/common/SectionContainer.astro";
import { checkAuth } from "../lib/auth";
const { isAuth } = await checkAuth(Astro.cookies);

// After the requireAuth check
if (!isAuth) return Astro.redirect("/login");
---

<App>
  <!-- <DebugComponent/> -->
  <!-- Navigation -->
  <SectionContainer class="mx-6 flex flex-col gap-y-6">
    <PushNotificationDemo />
  </SectionContainer>
</App>
