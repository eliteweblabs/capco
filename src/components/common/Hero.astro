---
// Hero section component

interface Props {
  title: string;
  description: string;
  globalPrimaryTextClasses?: string;
  globalSecondaryTextClasses?: string;
  globalInputClasses?: string;
}

const {
  title,
  description,
  globalPrimaryTextClasses,
  globalSecondaryTextClasses,
  globalInputClasses,
} = Astro.props;
---

<div class="mveJTCIb2WII7J4sY22F _wYiJGbRZyFZeCc8y7Sf LSIxp7RSuOKhsg28v_u3 col-span-12">
  <div class="EWLTGduHCjFnjN6tLCXV Q_jg_EPdNf9eDMn1mLI2 sJNGKHxFYdN5Nzml5J2j rvdRhGyExrNYTA6euxsF">
  </div>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <!-- Main Headline -->
      <h1 class={`${globalPrimaryTextClasses || ""} md:text-4xl sm:text-3xl text-2xl font-bold`}>
        {title}
      </h1>
      {
        description && (
          <h2 id="page-subtitle" class="text-black dark:text-white" set:html={description} />

          // <h2
          //   id="page-subtitle"
          //   class={`${globalSecondaryTextClasses || ""} mb-6 mt-6 text-xl sm:text-2xl md:text-3xl`}
          //   set:html={description}
          // />
        )
      }
    </div>
  </div>
</div>
