---
interface Props {
  name: string;
  class?: string;
  size?: string;
  color?: string;
  variant?: "regular" | "solid" | "logo";
  id?: string;
  style?: string;
}

const {
  name,
  class: className = "block size-4",
  size = "1rem",
  color = "currentColor",
  variant = "regular",
  id,
  style,
  ...rest
} = Astro.props;

const prefix = variant === "solid" ? "bxs" : variant === "logo" ? "bxl" : "bx";
const iconClass = `${prefix} ${prefix}-${name}`;
const iconStyle = style || `font-size: ${size}; color: ${color};`;
---

<i class={iconClass} style={iconStyle} class:list={[className]} id={id} {...rest}></i>
