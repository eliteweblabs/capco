---
// Flowbite Stats Card Component
// Based on Flowbite dashboard design patterns

import { globalClasses } from "../../pages/api/global/global-classes";

const { globalCardStyle } = globalClasses();

interface Props {
  title: string;
  value: string | number;
  change?: {
    value: string;
    type: "increase" | "decrease";
  };
  icon?: string;
  color?: "blue" | "green" | "yellow" | "red" | "purple" | "indigo";
  primaryTextClasses?: string;
  secondaryTextClasses?: string;
  globalInputClasses?: string;
}

const {
  title,
  value,
  change,
  icon = "chart-line",
  color = "blue",
  primaryTextClasses,
  secondaryTextClasses,
  globalInputClasses,
} = Astro.props;

const colorClasses = {
  blue: "bg-primary-50 text-primary-600 dark:bg-primary-900/50 dark:text-primary-400",
  green: "bg-green-50 text-green-600 dark:bg-green-900/50 dark:text-green-400",
  yellow: "bg-yellow-50 text-yellow-600 dark:bg-yellow-900/50 dark:text-yellow-400",
  red: "bg-red-50 text-red-600 dark:bg-red-900/50 dark:text-red-400",
  purple: "bg-purple-50 text-purple-600 dark:bg-purple-900/50 dark:text-purple-400",
  indigo: "bg-indigo-50 text-indigo-600 dark:bg-indigo-900/50 dark:text-indigo-400",
};

const changeColorClasses = {
  increase: "text-green-600 dark:text-green-400",
  decrease: "text-red-600 dark:text-red-400",
};
---
<div
  class={`${globalCardStyle} p-6 shadow dark:shadow-gray-700`}
>
  <div class="flex items-center justify-between">
    <div>
      <p class="mb-1 text-sm font-medium text-gray-500 dark:text-gray-400">
        {title}
      </p>
      <p class="text-2xl font-bold text-gray-900 dark:text-white">
        {value}
      </p>
      {
        change && (
          <div class="mt-2 flex items-center">
            <span class={`text-sm font-medium ${changeColorClasses[change.type]}`}>
              {change.type === "increase" ? "+" : "-"}
              {change.value}
            </span>
            <span class="ml-1 text-sm text-gray-500 dark:text-gray-400">vs last month</span>
          </div>
        )
      }
    </div>
    <div class={`p-3 rounded-full ${colorClasses[color]}`}>
      {icon}
    </div>
  </div>
</div>
