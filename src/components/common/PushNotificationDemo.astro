---
// Push Notification Demo Component
---

<div class="_1jTZ8KXRZul60S6czNi rounded-full bg-background-card p-6 shadow">
  <h3 class="mb-4 text-lg font-semibold">🔔 Push Notification Demo</h3>

  <div class="space-y-4">
    <!-- Notification Permission Status -->
    <div id="permission-status" class="rounded-full bg-gray-100 p-3 dark:bg-gray-700">
      <p class="text-sm text-gray-600 dark:text-gray-400">Checking notification permissions...</p>
    </div>

    <!-- Controls -->
    <div class="flex flex-wrap gap-3">
      <button
        id="request-permission-btn"
        class="rounded bg-blue-500 px-4 py-2 text-white hover:bg-blue-600 disabled:opacity-50"
        disabled
      >
        Request Permission
      </button>

      <button
        id="test-local-push-btn"
        class="rounded bg-green-500 px-4 py-2 text-white hover:bg-green-600 disabled:opacity-50"
        disabled
      >
        Test Local Push
      </button>

      <button
        id="test-project-update-btn"
        class="rounded bg-purple-500 px-4 py-2 text-white hover:bg-purple-600 disabled:opacity-50"
        disabled
      >
        Demo: Project Update
      </button>

      <button
        id="test-deadline-reminder-btn"
        class="rounded bg-orange-500 px-4 py-2 text-white hover:bg-orange-600 disabled:opacity-50"
        disabled
      >
        Demo: Deadline Reminder
      </button>
    </div>

    <!-- Installation Instructions for iOS -->
    <div
      id="ios-instructions"
      class="hidden rounded-full border border-blue-200 bg-blue-50 p-4 dark:border-blue-800 dark:bg-blue-900/20"
    >
      <h4 class="mb-2 font-semibold text-blue-800 dark:text-blue-200">
        📱 iOS Users: Enable Push Notifications
      </h4>
      <p class="mb-3 text-sm text-blue-700 dark:text-blue-300">
        <strong>Push notifications don't work in Safari browser tabs.</strong> You need to install this
        as a web app:
      </p>
      <ol class="list-inside list-decimal space-y-2 text-sm text-blue-700 dark:text-blue-300">
        <li>
          In Safari, tap the <strong>Share button</strong>
          <span
            class="inline-block rounded bg-blue-100 px-2 py-1 font-mono text-xs dark:bg-blue-800"
            >⬆️</span
          > at the bottom
        </li>
        <li>Scroll down and tap <strong>"Add to Home Screen"</strong></li>
        <li>Tap <strong>"Add"</strong> to install the Fire Protection App</li>
        <li><strong>Close Safari</strong> and open the new app icon from your home screen</li>
        <li>Navigate back to this demo page within the installed app</li>
        <li>Now the push notification buttons will work! 🎉</li>
      </ol>
      <div
        class="mt-3 rounded bg-blue-100 p-2 text-xs text-blue-600 dark:bg-blue-800 dark:text-blue-200"
      >
        <strong>💡 Why this works:</strong> iOS only allows push notifications in installed web apps
        (PWAs), not in Safari browser tabs. This is Apple's security policy.
      </div>
    </div>

    <!-- Debug Info -->
    <details class="text-sm">
      <summary class="cursor-pointer text-gray-600 dark:text-gray-400">🛠️ Debug Info</summary>
      <div id="debug-info" class="mt-2 rounded bg-gray-50 p-3 font-mono text-xs dark:bg-gray-900">
        Loading...
      </div>
    </details>
  </div>
</div>

<script>
  // Check if push notifications are supported
  function isPushSupported() {
    return "serviceWorker" in navigator && "Notification" in window && "PushManager" in window;
  }

  // Check if running on iOS
  function isIOS() {
    return /iPad|iPhone|iPod/.test(navigator.userAgent);
  }

  // Check if running as standalone app (PWA)
  function isStandalone() {
    return (
      window.matchMedia("(display-mode: standalone)").matches ||
      (window.navigator as any).standalone === true
    );
  }

  // Update permission status display
  function updatePermissionStatus() {
    const statusDiv = document.getElementById("permission-status");
    const permissionState = "Notification" in window ? Notification.permission : "Not available";
    const isSupported = isPushSupported();
    const ios = isIOS();
    const standalone = isStandalone();

    let statusHTML = `
      <p><strong>Browser:</strong> ${navigator.userAgent.includes("Chrome") ? "Chrome" : navigator.userAgent.includes("Firefox") ? "Firefox" : navigator.userAgent.includes("Safari") ? "Safari" : "Other"}</p>
      <p><strong>Platform:</strong> ${ios ? "iOS" : "Desktop/Android"}</p>
      <p><strong>Push Support:</strong> ${isSupported ? "✅ Yes" : "❌ No"}</p>
      <p><strong>Permission:</strong> ${permissionState}</p>
    `;

    if (ios) {
      statusHTML += `<p><strong>App Mode:</strong> ${standalone ? "✅ PWA (Push notifications enabled)" : "❌ Safari Tab (Push notifications disabled)"}</p>`;
      if (!standalone) {
        document.getElementById("ios-instructions")?.classList.remove("hidden");
      } else {
        // Add a success message when in PWA mode
        statusHTML += `<div class="mt-2 p-2 bg-green-100 rounded text-xs text-green-700 dark:bg-green-800 dark:text-green-200">🎉 Perfect! You're using the installed app. Push notifications will work!</div>`;
      }
    }

    if (statusDiv) statusDiv.innerHTML = statusHTML;

    // Enable/disable buttons based on status
    // Allow request button for both "default" and "denied" states (user can try again)
    const canRequest =
      isSupported &&
      (permissionState === "default" || permissionState === "denied") &&
      (!ios || standalone);
    const canSend = isSupported && permissionState === "granted" && (!ios || standalone);

    (document.getElementById("request-permission-btn") as HTMLButtonElement)!.disabled =
      !canRequest;
    (document.getElementById("test-local-push-btn") as HTMLButtonElement)!.disabled = !canSend;
    (document.getElementById("test-project-update-btn") as HTMLButtonElement)!.disabled = !canSend;
    (document.getElementById("test-deadline-reminder-btn") as HTMLButtonElement)!.disabled =
      !canSend;
  }

  // Update debug info
  function updateDebugInfo() {
    const debugDiv = document.getElementById("debug-info");
    const info = {
      userAgent: navigator.userAgent,
      platform: navigator.platform,
      cookieEnabled: navigator.cookieEnabled,
      onLine: navigator.onLine,
      language: navigator.language,
      standalone: isStandalone(),
      displayMode: window.matchMedia("(display-mode: standalone)").matches
        ? "standalone"
        : "browser",
      notificationPermission: "Notification" in window ? Notification.permission : "Not available",
      pushSupported: isPushSupported(),
      serviceWorkerSupported: "serviceWorker" in navigator,
      location: window.location.href,
      timestamp: new Date().toISOString(),
    };

    if (debugDiv) {
      debugDiv.innerHTML = JSON.stringify(info, null, 2);
    }
  }

  // Request notification permission
  async function requestPermission() {
    try {
      // console.log("🔔 [PUSH-DEMO] Starting permission request...");
      // console.log(
        "🔔 [PUSH-DEMO] Current permission:",
        "Notification" in window ? Notification.permission : "Not available"
      );
      // console.log("🔔 [PUSH-DEMO] Notification supported:", "Notification" in window);
      // console.log("🔔 [PUSH-DEMO] Is PWA:", window.navigator.standalone);

      // Check if we can actually request permission
      if (!("Notification" in window)) {
        alert("This browser doesn't support notifications");
        return;
      }

      // If already granted or denied, show appropriate message
      if ("Notification" in window && Notification.permission === "granted") {
        alert("✅ Notifications are already enabled!");
        updatePermissionStatus();
        return;
      }

      if ("Notification" in window && Notification.permission === "denied") {
        alert(
          "❌ Notifications are blocked. To enable:\n\n1. Tap the 'aA' icon in Safari's address bar\n2. Tap 'Website Settings'\n3. Enable 'Notifications'\n4. Refresh this page"
        );
        return;
      }

      // Request permission
      // console.log("🔔 [PUSH-DEMO] Requesting permission...");
      const permission = await Notification.requestPermission();
      // console.log("🔔 [PUSH-DEMO] Permission result:", permission);

      updatePermissionStatus();

      if (permission === "granted") {
        alert("✅ Notifications enabled! You can now test push notifications.");
        // Also register for push if we want remote notifications later
        const registration = await navigator.serviceWorker.getRegistration();
        if (registration && registration.pushManager) {
          try {
            const subscription = await registration.pushManager.subscribe({
              userVisibleOnly: true,
              applicationServerKey: null, // We'll add VAPID keys later for remote push
            });
            // console.log("Push subscription:", subscription);
          } catch (pushError) {
            // console.log(
              "Push subscription failed (this is OK for local notifications):",
              pushError
            );
          }
        }
      } else if (permission === "denied") {
        alert(
          "❌ Notifications blocked. You can enable them in Safari settings and refresh this page."
        );
      } else {
        alert("⚠️ Permission request was dismissed. Try again when ready.");
      }
    } catch (error: any) {
      console.error("❌ [PUSH-DEMO] Error requesting permission:", error);
      alert("Error requesting permission: " + error.message);
    }
  }

  // Send a local push notification
  function sendLocalPush(title: string, body: string, icon?: string) {
    // console.log("🔔 [PUSH-DEMO] sendLocalPush called with:", { title, body, icon });
    // console.log(
      "🔔 [PUSH-DEMO] Notification permission:",
      "Notification" in window ? Notification.permission : "Not available"
    );

    if ("Notification" in window && Notification.permission === "granted") {
      // console.log("🔔 [PUSH-DEMO] Creating notification...");
      try {
        const notification = new Notification(title, {
          body: body,
          icon: icon || "/favicon.svg",
          badge: "/favicon.svg",
          tag: "capco-demo",
          requireInteraction: false,
        });

        // console.log("✅ [PUSH-DEMO] Notification created successfully");

        // Auto-close after 5 seconds
        setTimeout(() => {
          // console.log("🔔 [PUSH-DEMO] Auto-closing notification");
          notification.close();
        }, 5000);

        notification.onclick = function () {
          // console.log("🔔 [PUSH-DEMO] Notification clicked");
          window.focus();
          notification.close();
        };
      } catch (error) {
        console.error("❌ [PUSH-DEMO] Error creating notification:", error);
      }
    } else {
      // console.log(
        "❌ [PUSH-DEMO] Notification permission not granted:",
        "Notification" in window ? Notification.permission : "Not available"
      );
    }
  }

  // Initialize the component
  function initPushDemo() {
    // console.log("🔔 [PUSH-DEMO] Initializing push notification demo...");

    updatePermissionStatus();
    updateDebugInfo();

    // Set up event listeners with debugging
    const requestBtn = document.getElementById("request-permission-btn") as HTMLButtonElement;
    // console.log(
      "🔔 [PUSH-DEMO] Request button found:",
      !!requestBtn,
      "disabled:",
      requestBtn?.disabled
    );
    requestBtn?.addEventListener("click", () => {
      // console.log("🔔 [PUSH-DEMO] Request permission button clicked");
      requestPermission();
    });

    const testLocalBtn = document.getElementById("test-local-push-btn") as HTMLButtonElement;
    // console.log(
      "🔔 [PUSH-DEMO] Test local button found:",
      !!testLocalBtn,
      "disabled:",
      testLocalBtn?.disabled
    );
    testLocalBtn?.addEventListener("click", () => {
      // console.log("🔔 [PUSH-DEMO] Test local push button clicked");
      sendLocalPush("🔔 Test Notification", "This is a test push notification from CAPCo Fire!");
    });

    const testProjectBtn = document.getElementById("test-project-update-btn") as HTMLButtonElement;
    // console.log(
      "🔔 [PUSH-DEMO] Test project button found:",
      !!testProjectBtn,
      "disabled:",
      testProjectBtn?.disabled
    );
    testProjectBtn?.addEventListener("click", () => {
      // console.log("🔔 [PUSH-DEMO] Test project update button clicked");
      sendLocalPush(
        "📋 Project Update",
        "Your fire protection project status has been updated to 'Under Review'",
        "/img/capco-icon.svg"
      );
    });

    const testDeadlineBtn = document.getElementById(
      "test-deadline-reminder-btn"
    ) as HTMLButtonElement;
    // console.log(
      "🔔 [PUSH-DEMO] Test deadline button found:",
      !!testDeadlineBtn,
      "disabled:",
      testDeadlineBtn?.disabled
    );
    testDeadlineBtn?.addEventListener("click", () => {
      // console.log("🔔 [PUSH-DEMO] Test deadline reminder button clicked");
      sendLocalPush(
        "⏰ Deadline Reminder",
        "Don't forget: Your project documentation is due in 2 days",
        "/img/capco-icon.svg"
      );
    });

    // Update status periodically
    setInterval(() => {
      updatePermissionStatus();
      updateDebugInfo();
    }, 2000);
  }

  // Run when DOM is ready
  // console.log("🔔 [PUSH-DEMO] Script loaded, document.readyState:", document.readyState);

  if (document.readyState === "loading") {
    // console.log("🔔 [PUSH-DEMO] DOM still loading, adding event listener");
    document.addEventListener("DOMContentLoaded", () => {
      // console.log("🔔 [PUSH-DEMO] DOMContentLoaded fired");
      initPushDemo();
    });
  } else {
    // console.log("🔔 [PUSH-DEMO] DOM already loaded, initializing immediately");
    initPushDemo();
  }
</script>
