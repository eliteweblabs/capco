---
// Cal.com Booking Component
// This component integrates with Cal.com for real appointment booking
---

<div class="grid grid-cols-1 gap-8 md:grid-cols-2">
  <!-- Cal.com Embed Section -->
  <div class="border-border-light dark:border-border-dark rounded-lg border p-6 dark:bg-gray-800">
    <h3 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">Schedule Your Demo</h3>
    <p class="mb-6 text-sm text-gray-600 dark:text-gray-400">
      Choose a convenient time for your personalized demo. We'll send you a calendar invite and
      meeting details.
    </p>

    <!-- Cal.com Embed Container -->
    <div id="cal-com-embed" class="min-h-[600px]">
      <div class="flex items-center justify-center h-64 bg-gray-50 dark:bg-gray-700 rounded-lg">
        <div class="text-center">
          <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4">
          </div>
          <p class="text-gray-600 dark:text-gray-400">Loading calendar...</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Booking Information -->
  <div class="border-border-light dark:border-border-dark rounded-lg border p-6 dark:bg-gray-800">
    <h3 class="mb-4 text-lg font-semibold text-gray-900 dark:text-white">What to Expect</h3>

    <div class="space-y-4">
      <div class="flex items-start space-x-3">
        <div class="flex-shrink-0">
          <div
            class="w-8 h-8 bg-blue-100 dark:bg-blue-900 rounded-full flex items-center justify-center"
          >
            <svg
              class="w-4 h-4 text-blue-600 dark:text-blue-400"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
        <div>
          <h4 class="text-sm font-medium text-gray-900 dark:text-white">30-Minute Demo</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            Focused walkthrough of key features
          </p>
        </div>
      </div>

      <div class="flex items-start space-x-3">
        <div class="flex-shrink-0">
          <div
            class="w-8 h-8 bg-green-100 dark:bg-green-900 rounded-full flex items-center justify-center"
          >
            <svg
              class="w-4 h-4 text-green-600 dark:text-green-400"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
        <div>
          <h4 class="text-sm font-medium text-gray-900 dark:text-white">Personalized Experience</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">Tailored to your specific needs</p>
        </div>
      </div>

      <div class="flex items-start space-x-3">
        <div class="flex-shrink-0">
          <div
            class="w-8 h-8 bg-purple-100 dark:bg-purple-900 rounded-full flex items-center justify-center"
          >
            <svg
              class="w-4 h-4 text-purple-600 dark:text-purple-400"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"
              ></path>
            </svg>
          </div>
        </div>
        <div>
          <h4 class="text-sm font-medium text-gray-900 dark:text-white">Q&A Session</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">
            Get answers to your specific questions
          </p>
        </div>
      </div>

      <div class="flex items-start space-x-3">
        <div class="flex-shrink-0">
          <div
            class="w-8 h-8 bg-orange-100 dark:bg-orange-900 rounded-full flex items-center justify-center"
          >
            <svg
              class="w-4 h-4 text-orange-600 dark:text-orange-400"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 17h5l-5 5v-5zM4 19h6v-2H4v2zM4 5h6V3H4v2zM4 11h6V9H4v2z"></path>
            </svg>
          </div>
        </div>
        <div>
          <h4 class="text-sm font-medium text-gray-900 dark:text-white">Next Steps</h4>
          <p class="text-sm text-gray-600 dark:text-gray-400">Clear path forward after the demo</p>
        </div>
      </div>
    </div>

    <!-- Contact Information -->
    <div class="mt-8 pt-6 border-t border-gray-200 dark:border-gray-700">
      <h4 class="text-sm font-medium text-gray-900 dark:text-white mb-3">Need Help?</h4>
      <div class="space-y-2 text-sm text-gray-600 dark:text-gray-400">
        <p>
          📧 <a
            href="mailto:support@capcofire.com"
            class="text-blue-600 hover:text-blue-800 dark:text-blue-400">support@capcofire.com</a
          >
        </p>
        <p>
          📞 <a href="tel:+1-555-0123" class="text-blue-600 hover:text-blue-800 dark:text-blue-400"
            >(555) 012-3456</a
          >
        </p>
        <p>
          💬 <a href="/contact" class="text-blue-600 hover:text-blue-800 dark:text-blue-400"
            >Live Chat</a
          >
        </p>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const calEmbedContainer = document.getElementById("cal-com-embed");

    if (!calEmbedContainer) {
      console.error("Cal.com embed container not found");
      return;
    }

    // Cal.com configuration
    const calConfig = {
      // Replace with your actual Cal.com username/event type
      calLink: "capco-fire/demo", // This should be your Cal.com username/event
      layout: "month_view",
      theme: "light", // or "dark" based on user preference
      branding: {
        brandColor: "#2563eb", // Your brand color
        hideEventTypeDetails: false,
        hideLandingPageDetails: false,
      },
      // Optional: Pre-fill form data if user is logged in
      prefill: {
        name:
          (window as any).currentUser?.profile?.firstName +
            " " +
            (window as any).currentUser?.profile?.lastName || "",
        email: (window as any).currentUser?.email || "",
        company: (window as any).currentUser?.profile?.companyName || "",
      },
    };

    // Load Cal.com embed script
    function loadCalComEmbed() {
      // Check if Cal.com script is already loaded
      if ((window as any).Cal) {
        initializeCalEmbed();
        return;
      }

      // Load Cal.com embed script
      const script = document.createElement("script");
      script.src = "https://app.cal.com/embed/embed.js";
      script.async = true;
      script.onload = () => {
        console.log("✅ Cal.com script loaded");
        initializeCalEmbed();
      };
      script.onerror = () => {
        console.error("❌ Failed to load Cal.com script");
        showFallbackBooking();
      };

      document.head.appendChild(script);
    }

    function initializeCalEmbed() {
      try {
        // Clear loading state
        if (calEmbedContainer) {
          calEmbedContainer.innerHTML = "";
        }

        // Initialize Cal.com embed
        if ((window as any).Cal) {
          (window as any).Cal("init", calConfig);
          (window as any).Cal("inline", {
            elementOrSelector: calEmbedContainer,
            calLink: calConfig.calLink,
            layout: calConfig.layout,
            theme: calConfig.theme,
            branding: calConfig.branding,
            prefill: calConfig.prefill,
          });

          console.log("✅ Cal.com embed initialized");
        } else {
          console.error("❌ Cal.com not available");
          showFallbackBooking();
        }
      } catch (error) {
        console.error("❌ Error initializing Cal.com embed:", error);
        showFallbackBooking();
      }
    }

    function showFallbackBooking() {
      if (calEmbedContainer) {
        calEmbedContainer.innerHTML = `
        <div class="text-center py-12">
          <div class="w-16 h-16 bg-red-100 dark:bg-red-900 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-red-600 dark:text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.732-.833-2.5 0L4.268 19.5c-.77.833.192 2.5 1.732 2.5z"></path>
            </svg>
          </div>
          <h3 class="text-lg font-medium text-gray-900 dark:text-white mb-2">Calendar Unavailable</h3>
          <p class="text-gray-600 dark:text-gray-400 mb-4">We're having trouble loading our booking calendar.</p>
          <div class="space-y-2">
            <p class="text-sm text-gray-600 dark:text-gray-400">
              Please contact us directly to schedule your demo:
            </p>
            <div class="flex flex-col sm:flex-row gap-2 justify-center">
              <a href="mailto:support@capcofire.com" 
                 class="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                📧 Email Us
              </a>
              <a href="tel:+1-555-0123" 
                 class="inline-flex items-center px-4 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 transition-colors">
                📞 Call Us
              </a>
            </div>
          </div>
        </div>
      `;
      }
    }

    // Start loading Cal.com embed
    loadCalComEmbed();
  });
</script>
