---
const {
  class: className = "",
  id,
  type = "default",
  fullWidth = false,
  py = "py-8 lg:px-6 lg:py-16",
} = Astro.props;

const typeClasses: Record<string, string> = {
  primary: "mx-auto px-4  bg-background-light dark:bg-background-dark",
  secondary: "mx-auto px-4  color-background",
  tertiary: "mx-auto px-4 bg-gray-50 dark:bg-gray-900",
  default: "mx-auto px-4 bg-background-light dark:bg-background-dark",
};
---

<div {id} data-section={id} class="px-4 pt-4">
  {
    (
      // fullWidth ? (
      <div>
        <slot />
      </div>
      // ) : (
      //   <div class="mx-auto max-w-screen-xl">
      //     <slot />
      //   </div>
      // )
    )
  }
</div>
