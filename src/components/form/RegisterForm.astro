---
import MultiStepForm from "./MultiStepForm.astro";
import { registerFormConfig } from "../../lib/forms/register-form-config";

interface Props {
  globalInputClasses?: string;
}

Astro.props; // accept for API compatibility with AuthForm
---

<MultiStepForm
  config={registerFormConfig}
  initialData={{ redirect: "/project/dashboard" }}
  containerClass="max-w-md"
/>
