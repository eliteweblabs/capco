---
/**
 * Layout: Default
 * Standard content page with container and full app chrome.
 * Add prose classes where needed for markdown/content pages.
 */
interface Props {
  title: string;
  description?: string;
  lastUpdated?: string;
  currentUser?: any;
  session?: any;
  supabase?: any;
}

const { title, description = "", lastUpdated } = Astro.props;

import App from "../ui/App.astro";
---

<App {title} {description} layout="default">
  <div data-dynamic-height="minHeight">
    {
      lastUpdated === "auto" && (
        <div class="dark:bg-primary-900/20 mb-8 rounded-lg border border-primary-200 bg-primary-50 p-6 dark:border-primary-800">
          <p class="my-0 text-primary-800 dark:text-primary-200">
            <strong>Last Updated:</strong> {new Date().toLocaleDateString()}
          </p>
        </div>
      )
    }

    <slot />
  </div>
</App>
