# JSON Multi-Step Form Generator - Visual Guide

## 🎨 Form Anatomy

```
┌─────────────────────────────────────────────┐
│           Form Title (optional)             │
│       Form Description (optional)           │
├─────────────────────────────────────────────┤
│   ████████░░░░░░░░░░░░░░░ 25%              │  Progress Bar
│              2 / 8                          │
├─────────────────────────────────────────────┤
│                                             │
│     Icon (optional)                         │
│                                             │
│        What's your name?                    │  Step Title
│     Optional subtitle text                  │  Step Subtitle
│                                             │
│   ┌─────────────┐  ┌─────────────┐        │
│   │   Field 1   │  │   Field 2   │        │  Fields
│   └─────────────┘  └─────────────┘        │
│                                             │
│   ┌─────────────────────────────┐         │
│   │        Field 3 (full)       │         │
│   └─────────────────────────────┘         │
│                                             │
│  [← Back]              [Next →]            │  Navigation
│                                             │
└─────────────────────────────────────────────┘
```

## 📦 Configuration Structure

```json
{
  "id": "form-id",              // ← Required: Unique identifier
  "action": "/api/submit",      // ← Required: Where to submit
  
  "title": "Form Title",        // ← Optional: Main heading
  "description": "...",         // ← Optional: Subtitle
  
  "progressBar": { ... },       // ← Optional: Progress config
  "successRedirect": "/done",   // ← Optional: Where to go after
  "successMessage": { ... },    // ← Optional: Success modal
  
  "steps": [                    // ← Required: Array of steps
    {
      "step": 1,                // ← Required: Step number
      "title": "Question?",     // ← Required: Step heading
      "icon": "user",           // ← Optional: BoxIcon name
      "layout": "grid-2",       // ← Optional: Layout type
      
      "fields": [               // ← Required: Array of fields
        {
          "name": "fieldName",  // ← Required
          "type": "text",       // ← Required
          "required": true      // ← Optional validation
        }
      ],
      
      "buttons": { ... }        // ← Optional: Custom buttons
    }
  ]
}
```

## 🎯 Field Type Visual Reference

### Text Inputs
```
┌─────────────────────────────────┐
│ John                            │  type: "text"
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ user@example.com                │  type: "email"
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ (555) 123-4567                  │  type: "tel"
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ••••••••                        │  type: "password"
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 42                              │  type: "number"
└─────────────────────────────────┘
```

### Select & Textarea
```
┌─────────────────────────────────┐
│ Select an option...         ▼   │  type: "select"
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ Multi-line                      │  type: "textarea"
│ text input                      │
│ here...                         │
└─────────────────────────────────┘
```

### Button Choice
```
  [  Yes  ]   [  No  ]              type: "button-choice"
  (selected)  (unselected)
```

### Special Components
```
┌─────────────────────────────────┐
│ (555) 123-4567                  │
│ ☑ Receive SMS notifications     │  type: "phone-sms"
│   [ Select Carrier ]            │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 123 Main St, Boston, MA...  🔍  │  type: "address-search"
│ ▼ Autocomplete dropdown         │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ [ Select Option... ]            │  type: "slot-machine"
│   (opens modal selector)        │
└─────────────────────────────────┘
```

## 🎨 Layout Options

### Single Column (default)
```
layout: "single"

┌─────────────────────────┐
│   Field 1 (full width)  │
└─────────────────────────┘
┌─────────────────────────┐
│   Field 2 (full width)  │
└─────────────────────────┘
┌─────────────────────────┐
│   Field 3 (full width)  │
└─────────────────────────┘
```

### Two Columns
```
layout: "grid-2"

┌───────────┐ ┌───────────┐
│  Field 1  │ │  Field 2  │
└───────────┘ └───────────┘
┌───────────────────────────┐
│   Field 3 (colSpan: 2)    │
└───────────────────────────┘
```

### Three Columns
```
layout: "grid-3"

┌────────┐ ┌────────┐ ┌────────┐
│ Field1 │ │ Field2 │ │ Field3 │
└────────┘ └────────┘ └────────┘
```

## 🔘 Button Variants

```
┌─────────────┐
│   Primary   │  variant: "primary"
└─────────────┘  (solid, brand color)

┌─────────────┐
│  Secondary  │  variant: "secondary"
└─────────────┘  (solid, secondary color)

┌─────────────┐
│   Outline   │  variant: "outline"
└─────────────┘  (transparent with border)

┌─────────────┐
│    Ghost    │  variant: "ghost"
└─────────────┘  (transparent, hover effect)

 Back           variant: "anchor"
                (link style)
```

## 📏 Button Sizes

```
┌──────┐
│  sm  │  size: "sm"
└──────┘

┌─────────┐
│   md    │  size: "md"
└─────────┘

┌────────────┐
│     lg     │  size: "lg"
└────────────┘

┌─────────────────┐
│       xl        │  size: "xl"
└─────────────────┘
```

## 🎯 Common Patterns

### Pattern 1: Name Fields
```json
{
  "layout": "grid-2",
  "fields": [
    {
      "name": "firstName",
      "type": "text",
      "placeholder": "John",
      "required": true
    },
    {
      "name": "lastName",
      "type": "text",
      "placeholder": "Doe",
      "required": true
    }
  ]
}
```

Visual:
```
┌──────────────┐ ┌──────────────┐
│    John      │ │     Doe      │
└──────────────┘ └──────────────┘
```

### Pattern 2: Yes/No Question
```json
{
  "fields": [
    {
      "name": "consent",
      "type": "button-choice",
      "options": [
        { "value": "yes", "label": "Yes" },
        { "value": "no", "label": "No" }
      ]
    }
  ]
}
```

Visual:
```
     [  Yes  ]   [  No  ]
```

### Pattern 3: Contact Info
```json
{
  "layout": "grid-2",
  "fields": [
    {
      "name": "email",
      "type": "email",
      "colSpan": 2
    },
    {
      "name": "phone",
      "type": "tel"
    },
    {
      "name": "company",
      "type": "text"
    }
  ]
}
```

Visual:
```
┌─────────────────────────────┐
│   your.email@example.com    │
└─────────────────────────────┘
┌──────────────┐ ┌──────────────┐
│ (555) 123... │ │ Acme Corp    │
└──────────────┘ └──────────────┘
```

## 🎨 Step Flow Visualization

```
Step 1              Step 2              Step 3
┌──────┐           ┌──────┐           ┌──────┐
│ Name │  ─────►   │Email │  ─────►   │Phone │  ─────►  ...
└──────┘           └──────┘           └──────┘
  25%                50%                75%
```

### With Conditional Logic
```
Step 1              Step 2              Step 3              Step 4
┌──────┐           ┌──────┐           ┌──────┐           ┌──────┐
│Phone │  ─────►   │ SMS? │  ──Yes──► │Carrir│  ─────►   │ Done │
└──────┘           └──────┘           └──────┘           └──────┘
                       │
                      No
                       │
                       ▼
                   ┌──────┐
                   │ Done │
                   └──────┘
```

## 🎯 Field Properties Hierarchy

```
Field
├─ name (required) ──────────────► Form data key
├─ type (required) ──────────────► Input type
├─ placeholder ──────────────────► Hint text
├─ required ─────────────────────► Validation
├─ label ────────────────────────► Field label
├─ errorMessage ─────────────────► Custom error
├─ defaultValue ─────────────────► Initial value
├─ class ────────────────────────► Custom CSS
├─ validation
│  ├─ minLength ──────────────────► Min chars
│  ├─ maxLength ──────────────────► Max chars
│  ├─ min ────────────────────────► Min value
│  ├─ max ────────────────────────► Max value
│  └─ pattern ────────────────────► Regex
├─ layout
│  └─ colSpan ────────────────────► Grid columns
└─ conditional
   ├─ dependsOn ──────────────────► Watch field
   └─ showWhen ───────────────────► Show if value
```

## 🚀 Quick Start Flow

```
1. Create JSON          2. Import Component         3. Use Component
   config.json    →     import JSONForm     →      <JSONForm config={} />
                              ↓
                         4. Open Browser
                              ↓
                        5. Test Form! ✓
```

## 🎓 Learning Path

```
Beginner
└─ Start with examples
   └─ form-contact-example.json
   
Intermediate  
└─ Read Quick Reference
   └─ Try different field types
   
Advanced
└─ Read Full Documentation
   └─ Use conditional logic
   └─ Custom submit handlers
   └─ Multi-column layouts
```

## 📊 Complexity Comparison

### Before (Manual)
```
HTML:         ~200 lines
JavaScript:   ~300 lines
CSS:          ~50 lines
Total:        ~550 lines per form
Time:         2-4 hours
```

### After (JSON-Driven)
```
JSON:         ~80 lines
Total:        ~80 lines per form
Time:         15-30 minutes
Reduction:    85% less code
```

## ✅ Checklist for Creating Forms

```
□ Define form purpose
□ Sketch out steps (one topic per step)
□ List all required fields
□ Choose field types
□ Set validation rules
□ Design button flow
□ Add optional fields
□ Configure progress bar
□ Set success redirect
□ Test on mobile
□ Test validation
□ Test submission
```

## 🎨 Color Guide (Default Theme)

```
Primary:      #667eea  (blue-purple)
Secondary:    #764ba2  (purple)
Success:      #10b981  (green)
Error:        #ef4444  (red)
Warning:      #f59e0b  (orange)
Info:         #3b82f6  (blue)
```

---

**Tip**: Start simple, then add complexity as needed. The examples in `/config/` are great starting points! 🚀
